services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    image: mikrotik-app:latest
    container_name: mikrotik-app
    restart: unless-stopped
    volumes:
      - .:/app
    env_file:
      - .env
    networks:
      - shared

  # Background worker scheduler MikroTik (Reuse image dari service app agar tidak build 2x)
  scheduler:
    image: mikrotik-app:latest
    container_name: mikrotik-scheduler
    restart: unless-stopped
    volumes:
      - .:/app
    env_file:
      - .env
    command: ["php", "artisan", "schedule:work"]
    depends_on:
      - app
    networks:
      - shared

networks:
  shared:
    external: true
